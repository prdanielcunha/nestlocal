{
  "name": "nestlocal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "express": "5.1.0",
    "firebase-admin": "13.5.0",
    "multer": "2.0.2"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check src/domain/quote.mjs && node --check server.mjs && node --check web/live.js && node --test"
  }
}
